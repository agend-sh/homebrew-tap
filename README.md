# agend-sh/homebrew-tap

Homebrew tap for the [agend](https://agend.sh) CLI.

```sh
brew install agend-sh/tap/agend
```

The `Formula/agend.rb` file is auto-updated by GoReleaser on each CLI release.
